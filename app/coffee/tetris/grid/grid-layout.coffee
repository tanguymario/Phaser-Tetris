assert = require '../../utils/assert.coffee'

debug       = require '../../utils/debug.coffee'
debugThemes = require '../../utils/debug-themes.coffee'

class GridLayout
  constructor: (game, grid, view, config) ->
    @game = game
    @grid = grid
    @view = view
    


module.exports = GridLayout
